@{
  Layout = "_Layout";
}

@using ParksMVC.Models
@model List<ParksMVC.Models.Park>

<h1>Parks</h1>

@foreach(Park park in Model)
{
    <h3>@Html.ActionLink(@park.Name, "Details", "Parks", new { id = @park.ParkId })</h3>
}

<button class="btn">@Html.ActionLink("Add Park", "Create", "Parks")</button>
