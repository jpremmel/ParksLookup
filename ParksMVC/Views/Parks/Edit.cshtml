@{
  Layout = "_Layout";
}

@using ParksMVC.Models
@model ParksMVC.Models.Park

<h1>Edit Park Info</h1>

@using(Html.BeginForm())
{
  @Html.Label("Name", "Name: ")
  @Html.EditorFor(model => model.Name, new { @class="form-control", required="required" })
  <br>
  @Html.Label("Location", "Location: ")
  @Html.EditorFor(model => model.Location, new { @class="form-control", required="required" })
  <br>
  @Html.Label("SquareMileage", "Area: ")
  <p>@Html.EditorFor(model => model.SquareMileage, new { @class="form-control", type="number" }) mi^2</p>
  <br>
  @Html.Label("Terrain", "Terrain: ")
  @Html.EditorFor(model => model.Terrain, new { @class="form-control" })
  <br>
  @Html.Label("Description", "Description: ")
  @Html.EditorFor(model => model.Description, new { @class="form-control" })
  <br>
  <input type="submit" value="Update Park" class="btn btn-success">
}